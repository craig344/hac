to start with tables you need to install
1. npm i react-table --save
2. npm i react-html-table-to-excel --save //this is specially use to export data into a excel sheet. To do that you need to install this.